import Button from "@mui/material/Button";
import CssBaseline from "@mui/material/CssBaseline";
// 1. import the TextField component (https://mui.com/material-ui/react-text-field/)

function App() {
  return (
    <>
      <CssBaseline /> {/* Reset CSS */}
      <Button>Hello</Button>
      {/* 2. render the email TextField with:
        - an id of "email"
        - a label of "Email"
        - a placeholder of "Enter your email"
        - a type of "email"
        - other attributes that you think are necessary
       */}
    </>
  );
}

export default App;
