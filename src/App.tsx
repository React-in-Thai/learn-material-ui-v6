import Button from "@mui/material/Button";
// 1. import CSS Baseline from Material-UI

function App() {
  // 2. render CSS Baseline component (it should be at the top of the component tree)
  return <Button>Hello</Button>;
}

export default App;
